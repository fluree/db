[package]
name = "fluree-sse"
description = "Lightweight SSE (Server-Sent Events) parser for Fluree DB"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tracing = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
