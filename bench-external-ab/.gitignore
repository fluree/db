# Scratch/regenerated run artifacts. The raw JSONL behind every published RESULTS.md
# number is copied into the tracked results/ dir (see results/README.md); harness/out/
# stays ignored for local re-runs.
harness/out/
__pycache__/
*.pyc
# Local, generated fluree home (registered per-machine; not portable)
substrate/fluree-home/
