# PR #1453 — Review

## Inline comments and/or suggested edits

### Blocking

- **`WAVE3-HANDOFF-DOSSIER.md:3`** — *blocking.* This file's own header says "**This file is deliberately untracked** — share internally; do not commit," yet it is committed at the repo root and will merge to `main`. It entered through the `burndown/pr-w2a` merge commit (`e24694169`) as a merge-resolution add, which is why a plain `git log -- <file>` doesn't even show it. It carries internal orchestration/process detail (worktree choreography, agent hand-off notes, "AJ's standing rule") that doesn't belong in the public tree, and at the root rather than `docs/`. Fix: `git rm` it in a follow-up commit on the branch (or, if the team decides it *should* be tracked, re-home it under `docs/audit/burn-down/` and rewrite the header — but that's a conscious decision, not a default).
- **`fluree-db-sparql/src/parse/query/tests.rs:2928` + `fluree-db-transact/src/lower_sparql_update.rs:46`** — *blocking (enforced CI gate).* `cargo fmt --all -- --check` fails at HEAD on these two PR-introduced hunks (a doubled blank line; an import list rustfmt wants re-wrapped), verified with rustfmt 1.9.0-stable — the same stable CI installs. Both are format-stable behaviors, not version quirks. One `cargo fmt --all` fixes it. Related process note: **no CI-workflow run exists for this PR at all** (only the Release workflow's `plan` job triggered on the pull_request event), so the fmt/clippy/nextest/W3C gates never executed upstream and nothing would have caught this — after pushing the fmt fix, confirm the CI workflow actually runs and goes green before merging.

### Optional (recommended)

- **`fluree-db-transact/src/lower_sparql_update.rs:1090`** — *optional, recommended.* `rewrite_blank_nodes_to_vars` mints **lexable** variable names (`_fluree_bn_{label}`, `_fluree_bn_anon{N}`): a user can legally write `?_fluree_bn_x` in the same request, and `DELETE WHERE { GRAPH <g> { _:x <p> ?o . ?s <q> ?_fluree_bn_x } }` would silently unify the user's variable with the rewritten existential, changing which triples get deleted. The comment at `:1076` says the `_fluree_` namespace "is already reserved by the annotation machinery," but that machinery reserves only `?_fluree_del_ann_` *and* guards user-written occurrences (`parse/edge_annotations.rs:678`); nothing enforces the broader prefix on this path. The sibling triple-only path already solves this airtightly — `BlankNodeVarNamer` mints `_:{label}` / `_:b{N}` names (`:707-708`) that contain `:` and are therefore unlexable as user variables. Suggested edit: use the same `_:`-style (or `#`-prefixed, per `INTERNAL_VAR_PREFIX`) names here, after confirming the staged SPARQL-WHERE lowering round-trips non-lexable var names; likelihood of real-world collision is tiny, but the fix is one line and this code is brand new.
- **`fluree-db-sparql/src/validate/mod.rs:392`** — *optional.* The validator's stable-id exemption is `l.starts_with("fdb-")` while the lowering's is `stable_blank_node_sid_from_label(l).is_some()` (`lower_sparql_update.rs`), so a malformed id like `_:fdb-zzz` passes `validate()` and then fails at lowering with a message suggesting the user "use a Fluree stable _:fdb- id" — which they will believe they did. End-to-end it still rejects (correct), but consider tightening the validator to the same predicate under the `lowering` feature, or special-casing the message for malformed `fdb-` labels. The prefix-sync test at `:961` is good; this is about the *validity* half of the check, not the prefix.
- **PR metadata** — *optional.* (a) The title's "−123 registers" is the removals count; net is **−121** (123 removed, 2 honestly re-registered: `dawg-delete-insert-01b`, `update-reifier-08`) — worth one clarifying line in the body since the title feeds release notes. (b) The PR carries no labels, and `.github/release.yml` is label-driven: this is the reject-more wave with several user-visible breaking changes — apply `breaking-change` (+ `feat`) so the auto-generated release notes categorize it correctly.

### Observations (no action required)

- **`fluree-db-sparql/src/validate/bnode_scope.rs:17`** — V3 deliberately treats BIND/VALUES as non-boundaries although §18.2.2.6 ends a BGP at BIND; documented as under-reject-never-over-reject with the audit reference, and no W3C test discriminates it. Fine as shipped; noting so the boundary set isn't "discovered" as a bug later.
- **`fluree-db-sparql/src/lower/select.rs:606`** — sub-SELECT trailing VALUES joins before grouping rather than after HAVING; the comment states the approximation honestly and W3C inline2 (modifier-free) is the only conformance pressure today. If a subselect-VALUES-plus-GROUP-BY case ever matters, this is the line to revisit.
- The two new register entries are the right call: both tests were green for indefensible reasons (silent first-op-only truncation; silent bnode acceptance), and registering them with owning-PR comments is exactly what the both-ways-policed register system is for.

### Praise (preserve these)

- **`fluree-db-api/src/query/helpers.rs:284`** — the one-branch seam fix is the highest-leverage change in the wave; the commit body (`a58757310`) documenting why the alternative (withholding the AST in `parse_sparql`) was rejected is model decision hygiene.
- **`fluree-db-api/tests/it_transact_update.rs:2368`** — the `t == 1` control after the rejected two-op request is the assertion that actually proves "staged nothing," not just "errored." Most tests of this class forget it.
- **`fluree-db-api/tests/it_query_grouping.rs:331,421`** — pinning the JSON-LD *divergences* (grouped-list projection, bind-as-constraint) as passing tests, not just prose, is what will stop a future "shared checker" refactor from silently breaking Fluree-owned surfaces.
- **`fluree-db-transact/src/lower_sparql_update.rs:951`** — routing GRAPH-bearing DELETE WHERE through the existing Modify machinery while keeping the triple-only fast path byte-identical (and pinning both routes in unit tests) is the right altitude; no second graph-template path was invented.

## Top-level approve/disapprove review submission comment

**🔄 Request changes** — two mechanical items, with the substance already at approve quality.

This wave does exactly what it claims and the claims survive adversarial re-verification: the diagnostics-swallowing hole at the query seam is genuinely closed (every public query path funnels through the fixed helper; the update path already matched), #1438's silent-data-loss window is converted to a loud error with an end-to-end proof it stages nothing, the four validation passes are conservative in the right direction (under-reject, never over-reject) with the V5-must-live-in-the-parser argument holding up under scrutiny, GRAPH-in-DELETE-WHERE extends the existing Modify lowering rather than inventing a parallel path, and the reifier-form parser lands on the existing edge-annotation IR with enum widths preserved. The consciously-breaking changes (agg08/group06 rejections, dot strictness, bnode-in-DELETE on both surfaces) are each spec-grounded, migration-noted, and pinned by tests on both the rejecting SPARQL surface and the deliberately-diverging JSON-LD surface. I re-ran the gates rather than trusting the description: 36/36 W3C suites green with 0 stale registers (net −121: 123 removed, 2 honestly re-registered with owners), 2,148 + 2,410 nextest tests green, testsuite clippy `-D warnings` clean. Requesting changes on exactly two things: `cargo fmt --all -- --check` fails at HEAD on two PR-introduced hunks (an enforced CI gate — and note CI never actually ran on this PR, so nothing upstream caught it), and `WAVE3-HANDOFF-DOSSIER.md` — whose own header says "deliberately untracked; do not commit" — is committed at the repo root via the pr-w2a merge commit. Both are five-minute fixes; with them done and CI observed green, this is an approve.

Adherence to repo commitments:
- **Patterns/abstractions:** ✔ Extends `validate()`/`DiagCode`, the Modify graph-template lowering, and `Pattern::EdgeAnnotation`/`AnnotationTarget`; no parallel constructs; SPARQL↔JSON-LD parity honored with explicit, test-pinned divergences.
- **Performance (speed first, memory second):** ✔ Neutral — zero engine-crate files touched (verified against the diffstat, not the description); all work is parse/validate/staging-time; IR enum widths preserved by boxing; BSBM parse-entry numbers corroborate. No performance-degradation risk.
- **Testing:** ✔ New suites wired into real `grp_*` targets (verified against the `autotests = false` trap); W3C registers shrink 524 → 403 with both-way policing; parity *and* divergence pins present; #1438 verified end-to-end.
- **Conventions:** ⚠️ Commits are exemplary (conventional, multi-line, isolated V1/V2 for bisect, no AI attribution) and the compliance doc gained the UPDATE/transact parity row — but the fmt gate fails at HEAD, a self-described do-not-commit file is in the tree, and the PR lacks the release-notes labels for a breaking wave.

Verified locally at branch HEAD (4f36f8ad6): `cargo fmt --all -- --check` ✖ (2 files) · `cargo nextest run -p fluree-db-sparql -p fluree-db-transact -p fluree-db-query` ✔ 2,148 · `-p fluree-db-api` ✔ 2,410 · `testsuite-sparql`: `cargo test` ✔ 36/36 (0 failed, 0 stale), `clippy -D warnings` ✔ · register net recomputed from files (−121) · CI-workflow run for this PR: **absent**.

Blocking on: `cargo fmt --all`, removal (or conscious re-homing) of `WAVE3-HANDOFF-DOSSIER.md`, and one observed-green CI run — then merge.
