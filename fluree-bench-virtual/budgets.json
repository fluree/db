{
  "schema_version": 1,
  "_comment": "Perf regression budgets for `vbench compare --gate`. A query violates only when its observed hot wall exceeds its blessed baseline by more than `budget_pct` percent AND by at least `min_delta_ms` absolute (so a percent of a tiny baseline can't gate on noise). Cold runs are advisory-only (never gate). `overrides` is keyed by query id and wins over the tag-level defaults. Baselines are single-machine medians: re-bless locally (`vbench baseline --perf --run <fresh run>`) before gating on a different machine — see README `Baselines, budgets & gating`.",
  "default_budget_pct": {
    "native": 10.0,
    "virtual_hot": 20.0
  },
  "min_delta_ms": 50,
  "cold": "advisory",
  "_overrides_comment": "The loadTable-GET drift set (q002/q004/q022/q024/q030/q043) is network-flappy catalog latency, NOT engine — C2 §3/§4 measured up to ~3.8x run-to-run swing. Quarantined at a wider 300% budget so a Snowflake catalog-latency spike (backed by the compare auto-rerun) does not flap the gate, while a gross (>4x) regression still trips. Re-narrow if the catalog path stabilizes.",
  "overrides": {
    "q002": 300.0,
    "q004": 300.0,
    "q022": 300.0,
    "q024": 300.0,
    "q030": 300.0,
    "q043": 300.0
  }
}
