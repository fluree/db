# q039 count_gl_entries
# Intent: COUNT(*) over the GL fact.
# BI lineage (BI-18): "How many GL journal entries are there?"
# Predicates verified against enterprise.ttl — class edw:GlJournalEntry.
PREFIX edw: <http://ns.fluree.dev/edw#>
SELECT (COUNT(*) AS ?n) WHERE { ?s a edw:GlJournalEntry . }
